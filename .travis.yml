language: java
jdk:
  - openjdk11

# master 브랜치에 적용
branches:
  only:
    - master


#Travis CI Server Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

before_script: "chmod +x ./gradlew"

script: "./gradlew clean build"

#CI 실행완료시 메일알림
notifications:
  email:
    recipients:
      - kyhwesaq@gmail.com